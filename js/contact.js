document.addEventListener("DOMContentLoaded", function () {
    var honeypot = document.getElementById("findus");
    if (honeypot) {
        honeypot.parentNode.removeChild(honeypot);
    }
    var honeypot = document.getElementById("finduslab");
    if (honeypot) {
        honeypot.parentNode.removeChild(honeypot);
    }

});

